import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    width: "100%",
    height: 80,
    alignItems: "flex-end",
  },
  button: {
    marginRight: 30,
    marginTop: 10,
    alignItems: "flex-end",
    width: 60,
  },
});

export { styles };
